import React from "react";

const CheckoutHeaderBlock = ({ spanText }) => {
    return (
        <div className="header-block">
            <span>{spanText}</span>
        </div>
    );
}

export default CheckoutHeaderBlock;